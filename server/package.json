{
  "type": "module",
  "scripts": {
    "start": "node -r dotenv-flow/config server.js",
    "dev": "nodemon --exec npm start"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "debounce": "^1.2.1",
    "dotenv": "^8.6.0",
    "dotenv-flow": "^3.2.0",
    "express": "^4.17.2",
    "socket.io": "^4.4.1",
    "wemo-client": "^0.15.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "nodemonConfig": {
    "watch": [
      "server/"
    ],
    "ext": "js",
    "env": {
      "NODE_ENV": "development"
    }
  }
}
